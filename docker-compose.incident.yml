services:
  order-service:
    environment:
      DATABASE_URL: postgresql+psycopg2://postgres:postgres@broken-postgres:5432/microservices_db
      JWT_SECRET: change-me-in-production
      JWT_ALGORITHM: HS256
      PRODUCT_SERVICE_URL: http://product-service:8000
