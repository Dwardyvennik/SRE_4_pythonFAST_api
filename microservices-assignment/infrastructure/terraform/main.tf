terraform {
  required_version = ">= 1.5.0"
}

locals {
  project_name = var.project_name
  environment  = var.environment
}

# Placeholder infrastructure definition for the assignment.
# Extend this file with cloud resources such as a VPC, container registry,
# Kubernetes cluster, managed PostgreSQL, and monitoring resources.
